curl ## Run `remoji.sh` Directly via Curl

You can run the `remoji.sh` script directly from GitHub without cloning the repo by using:

```bash
curl -s https://raw.githubusercontent.com/JeyKul/remoji/master/remoji.sh | bash

